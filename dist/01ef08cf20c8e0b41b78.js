import"./style.css";import*as THREE from"three";const scene=new THREE.Scene,geometry=new THREE.BoxGeometry(1,1,1),material=new THREE.MeshBasicMaterial({color:14986619}),cubeMesh=new THREE.Mesh(geometry,material);cubeMesh.rotation.x=120,cubeMesh.rotation.y=120,scene.add(cubeMesh);const sizes={width:window.innerWidth,height:window.innerHeight},camera=new THREE.PerspectiveCamera(75,sizes.width/sizes.height);scene.add(camera),camera.position.z=3;const canvas=document.querySelector(".webgl"),renderer=new THREE.WebGLRenderer({canvas});renderer.setSize(sizes.width,sizes.height),renderer.render(scene,camera);